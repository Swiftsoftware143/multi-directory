# Auto-deploy happens server-side via deploy-from-vps.sh or cron
# This is a placeholder — SSH secrets are not available for this repo.
# See /opt/swift/multidirectory-rust/deploy-from-vps.sh on the VPS
